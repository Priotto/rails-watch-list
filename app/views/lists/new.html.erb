<%# Primeiro o que está associado, depois o que é criado %>
<div class = "container">
  <%= simple_form_for (@list) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit %>
  <% end %>
  <%= link_to "Back", lists_path%>
</div>
